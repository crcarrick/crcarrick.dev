import { BlockCode, CodeWrapper } from './BlockCode/BlockCode';
import { InlineCode } from './InlineCode/InlineCode';

export const Code = {
  Pre: CodeWrapper,
  Block: BlockCode,
  Inline: InlineCode,
};
